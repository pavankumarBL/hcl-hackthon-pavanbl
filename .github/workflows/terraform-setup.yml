name: Infrastructure Deployment (Terraform)

on:
  push:
    branches:
      - feature-hcl
  pull_request:
    branches:
      - feature-hcl

jobs:
  terraform:
    runs-on: ubuntu-latest

    permissions:
      id-token: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v2
      with:
        terraform_version: 1.3.0

    - name: Set up AWS credentials using OIDC
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: arn:aws:iam::539935451710:role/GitHubActionRole-pavanbl
        aws-region: us-east-1

    - name: Initialize Terraform
      run: |
        echo "present dir"
        pwd
        echo "---"
        ls
        cd terraform
        terraform init

    - name: Terraform Plan
      run: |
        cd terraform
        terraform plan -out=terraform.plan

    # - name: Terraform Apply
    #   run: terraform apply -auto-approve terraform.plan

    # - name: Upload Terraform State to S3
    #   run: |
    #     aws s3 cp terraform.tfstate s3://hcl-hackathon-terraform-state-pavanbl/terraform.tfstate
    #     aws s3 cp terraform.tfstate.backup s3://hcl-hackathon-terraform-state-pavanbl/terraform.tfstate.backup
